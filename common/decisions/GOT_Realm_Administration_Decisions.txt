
GEN_Empire_Manager = {

	show_hide_formables_decisions = {
        priority = 100
        visible = { always = yes }
		
		ai_will_do = {
			factor = 0
		}

        complete_effect = { 
            if = {
                limit = { has_country_flag = show_formables_decisions }
                clr_country_flag = show_formables_decisions
            }
            else = {
                set_country_flag = show_formables_decisions
            }
        }
    }
	

	show_hide_integration_decisions = {
        priority = 100
        visible = { always = yes }
		
		ai_will_do = {
			factor = 0
		}

        complete_effect = { 
            if = {
                limit = { has_country_flag = show_integration_decisions }
                clr_country_flag = show_integration_decisions
            }
            else = {
                set_country_flag = show_integration_decisions
            }
        }
    }
	
	BFN_invite_namesake_into_faction = {
	
		icon = generic_form_nation
		
		allowed = {
		}
	
        visible = {
			GOT_Has_Same_Leader = yes
			NOT = { has_country_flag = show_integration_decisions }
			NOT = { FROM = { is_in_faction_with = ROOT } }
        }
        target_trigger = {
			GOT_Has_Same_Leader = yes
        }
        cost = 30
		modifier = { }
		days_remove = 3
		fire_only_once = yes
		
		ai_will_do = {
			factor = 1000
		}

        available = {
			custom_trigger_tooltip = {
				tooltip = GOT_Has_Same_Leader_tt
				GOT_Has_Same_Leader = yes
			}
			NOT = { has_war_with = FROM }
			FROM = {
				surrender_progress < 0.01	
			}
			ROOT = {
				surrender_progress < 0.01	
			}
			ROOT = { is_faction_leader = yes }
            FROM = { is_in_faction = no }
        }
		complete_effect = {
		}
        remove_effect = {
			FROM = {
				country_event = generic.5
			}
        }
    }
	
	BFN_demand_namesake_submit = {
	
		icon = eng_trade_unions_support
		
		allowed = {
		}
	
        visible = {
			GOT_Has_Same_Leader = yes
			FROM = { is_subject = no }
			FROM = { is_in_faction_with = ROOT }
			NOT = { has_country_flag = show_integration_decisions }
        }
        target_trigger = {
			GOT_Has_Same_Leader = yes
        }
        cost = 75
		modifier = { 
			civilian_factory_use = 3
			#stability_weekly = -0.002
		}
		days_remove = 7
		fire_only_once = yes
		
		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				FROM = { is_ai = no }
			}
			modifier = {
				factor = 0
				ROOT = { tag = CRO }
				FROM = { tag = DRA }
			}
			modifier = {
				factor = 0
				ROOT = { tag = CRO }
				FROM = { tag = STO }
			}
		}

        available = {
			num_of_civilian_factories_available_for_projects > 2
			custom_trigger_tooltip = {
				tooltip = GOT_Has_Same_Leader_tt
				GOT_Has_Same_Leader = yes
			}
			FROM = { is_subject = no }
			FROM = { is_in_faction_with = ROOT }
			FROM = {
				surrender_progress < 0.01	
			}
			ROOT = {
				surrender_progress < 0.01	
			}
			OR = {
				ROOT = { is_faction_leader = yes }
				ROOT = {
					strength_ratio = {
						tag = FROM
						ratio > 1.9
					}
				}
				ROOT = {
					ic_ratio = {
						tag = FROM
						ratio > 1.9
					}
				}
				AND = {
					FROM = {
						has_country_flag = GOT_Bent_The_Knee_Flag
					}
					ROOT = {
						has_country_flag = GOT_Claimed_Iron_Throne_Flag
					}
				}
			}
        }
		complete_effect = {
		}
        remove_effect = {
			FROM = {
				country_event = GOT_Realm_Admin_Country.1
			}
        }
    }
	
	BFN_demand_namesake_integrate = {
	
		icon = eng_trade_unions_support
		
		allowed = {
		}
	
        visible = {
			GOT_Has_Same_Leader = yes
			FROM = { is_subject_of = ROOT }
			NOT = { has_country_flag = show_integration_decisions }
        }
        target_trigger = {
			GOT_Has_Same_Leader = yes
        }
        cost = 100
		modifier = { 
			civilian_factory_use = 5
			#stability_weekly = -0.005
		}
		days_remove = 30
		fire_only_once = yes
		
		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				FROM = { is_ai = no }
			}
		}

        available = {
			num_of_civilian_factories_available_for_projects > 4
			custom_trigger_tooltip = {
				tooltip = GOT_Has_Same_Leader_tt
				GOT_Has_Same_Leader = yes
			}
			FROM = { is_subject_of = ROOT }
			FROM = { is_in_faction_with = ROOT }
			FROM = {
				surrender_progress < 0.01	
			}
			ROOT = {
				surrender_progress < 0.01	
			}
			has_manpower > 999
			has_equipment = {
				infantry_equipment > 999
			}
        }
		complete_effect = {
			add_manpower = -1000
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -1000
			}
		}
        remove_effect = {
			FROM = {
				country_event = GOT_Realm_Admin_Country.4
			}
        }
    }
	
	BFN_demand_riverlands_submit = {
	
		icon = eng_trade_unions_support
		
		allowed = {
			tag = NOR
		}
	
        visible = {
			country_exists = RIV
			RIV = { is_subject = no }
			RIV = { is_in_faction_with = NOR }
			NOT = { has_country_flag = show_integration_decisions }
			has_global_flag = GOT_King_of_the_Trident_Flag
        }
        cost = 75
		modifier = { 
			civilian_factory_use = 3
			#stability_weekly = -0.002
		}
		days_remove = 7
		fire_only_once = yes
		
		ai_will_do = {
			factor = 1000
		}

        available = {
			num_of_civilian_factories_available_for_projects > 2
			has_country_leader = {
				name = "Robb Stark"
				ruling_only = yes
			}
			country_exists = RIV
			RIV = { is_subject = no }
			RIV = { is_in_faction_with = NOR }
			RIV = {
				surrender_progress < 0.01	
			}
			NOR = {
				surrender_progress < 0.01	
			}
			OR = {
				ROOT = { is_faction_leader = yes }
				ROOT = {
					strength_ratio = {
						tag = FROM
						ratio > 1.9
					}
				}
				ROOT = {
					ic_ratio = {
						tag = FROM
						ratio > 1.9
					}
				}
			}
			has_global_flag = GOT_King_of_the_Trident_Flag
			NOT = { has_war_with = CRO }
			NOT = { has_war_with = WES }
        }
		complete_effect = {
		}
        remove_effect = {
			RIV = {
				country_event = GOT_Realm_Admin_Country.1
			}
        }
    }
	
	BFN_demand_riverlands_integrate = {
	
		icon = eng_trade_unions_support
		
		allowed = {
			tag = NOR
		}
	
        visible = {
			country_exists = RIV
			RIV = { is_subject_of = NOR }
			NOT = { has_country_flag = show_integration_decisions }
			has_global_flag = GOT_King_of_the_Trident_Flag
        }
        cost = 100
		modifier = { 
			civilian_factory_use = 5
			#stability_weekly = -0.005
		}
		days_remove = 30
		fire_only_once = yes
		
		ai_will_do = {
			factor = 1000
		}

        available = {
			num_of_civilian_factories_available_for_projects > 4
			has_country_leader = {
				name = "Robb Stark"
				ruling_only = yes
			}
			country_exists = RIV
			RIV = { is_subject_of = NOR }
			RIV = { is_in_faction_with = NOR }
			RIV = {
				surrender_progress < 0.01	
			}
			NOR = {
				surrender_progress < 0.01	
			}
			has_manpower > 999
			has_equipment = {
				infantry_equipment > 999
			}
			has_global_flag = GOT_King_of_the_Trident_Flag
			NOT = { has_war_with = CRO }
			NOT = { has_war_with = WES }
        }
		complete_effect = {
			add_manpower = -1000
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -1000
			}
		}
        remove_effect = {
			RIV = {
				country_event = GOT_Realm_Admin_Country.4
			}
        }
    }
	
BFN_coring_states_targeted_decision = {
	icon = GFX_decision_eng_trade_unions_support
    state_target = yes
    target_root_trigger = {
    }
    target_array = controlled_states
    target_trigger = {
        FROM = {
			NOT = { is_core_of = ROOT }
			is_fully_controlled_by = ROOT
			is_owned_by = ROOT
			any_neighbor_state = {
				is_core_of = ROOT
				is_fully_controlled_by = ROOT
				is_owned_by = ROOT
			}
        }
    }
	available = {
		has_war = no
		num_of_civilian_factories_available_for_projects > 4
		has_manpower > 499
		has_equipment = {
			infantry_equipment > 499
		}
	}
	visible = {
			has_war = no
            NOT = { has_country_flag = show_integration_decisions }
			FROM = { 
				NOT = { is_core_of = ROOT } 
				#NOT = { has_state_flag = Coring_In_Progress } 
			}
       }
	ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_political_power < 100
			}
		}
	modifier = { 
		civilian_factory_use = 5 
		#stability_weekly = -0.001
	}
	days_remove = 30
    on_map_mode = map_and_decisions_view
    cost = 25
	
    complete_effect = {
		add_manpower = -500
		add_equipment_to_stockpile = {
			type = infantry_equipment
			amount = -500
		}
		#hidden_effect = {
		#	FROM = { 
		#		set_state_flag = Coring_In_Progress
		#	}
		#}
	}
	remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			FROM = {
				add_core_of = ROOT
				#clr_state_flag = Coring_In_Progress
			}
	}
}

BFN_coring_states_targeted_decision_remote = {
	icon = GFX_decision_SWE_set_navy_budget
    state_target = yes
    target_root_trigger = {
    }
    target_array = controlled_states
    target_trigger = {
        FROM = {
			NOT = { is_core_of = ROOT }
			is_fully_controlled_by = ROOT
			is_owned_by = ROOT
			is_coastal = yes
			all_neighbor_state = {
				NOT = {
					AND = {
						is_core_of = ROOT
						is_owned_by = ROOT
					}
				}
			}
        }
    }
	available = {
		has_war = no
		num_of_civilian_factories_available_for_projects > 19
		has_manpower > 1999
		has_equipment = {
			infantry_equipment > 1999
		}
		has_equipment = {
			convoy > 99
		}
	}
	visible = {
			has_war = no
            NOT = { has_country_flag = show_integration_decisions }
			FROM = { 
				NOT = { is_core_of = ROOT } 
				#NOT = { has_state_flag = Coring_In_Progress } 
			}
        }
	ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_political_power < 400
			}
			modifier = {
				factor = 0
				has_equipment = {
					convoy < 1000
				}
			}
		}	
	modifier = { 
		civilian_factory_use = 20
		#stability_weekly = -0.004
	}
	days_remove = 120
    on_map_mode = map_and_decisions_view
    cost = 100
    complete_effect = {
		add_manpower = -2000
		add_equipment_to_stockpile = {
			type = infantry_equipment
			amount = -2000
		}
		add_equipment_to_stockpile = {
			type = convoy
			amount = -100
		}
		#hidden_effect = {
		#	FROM = { 
		#		set_state_flag = Coring_In_Progress
		#	}
		#}
	}
	remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			FROM = {
				add_core_of = ROOT
				#clr_state_flag = Coring_In_Progress
			}
	}
}

BFN_coring_states_targeted_decision_at_war = {
	icon = GFX_decision_eng_trade_unions_support
    state_target = yes
    target_root_trigger = {
    }
    target_array = controlled_states
    target_trigger = {
        FROM = {
			NOT = { is_core_of = ROOT }
			is_fully_controlled_by = ROOT
			is_owned_by = ROOT
			any_neighbor_state = {
				is_core_of = ROOT
				is_fully_controlled_by = ROOT
				is_owned_by = ROOT
			}
        }
    }
	available = {
		has_war = yes
		num_of_civilian_factories_available_for_projects > 7
		has_manpower > 749
		has_equipment = {
			infantry_equipment > 749
		}
	}
	visible = {
			has_war = yes
            NOT = { has_country_flag = show_integration_decisions }
			FROM = { 
				NOT = { is_core_of = ROOT } 
				#NOT = { has_state_flag = Coring_In_Progress } 
			}
       }
	ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_political_power < 120
			}
			modifier = {
				factor = 0
				surrender_progress > 0.05
			}
		}
	modifier = { 
		civilian_factory_use = 8
		#stability_weekly = -0.002
	}
	days_remove = 45
    on_map_mode = map_and_decisions_view
    cost = 38
	
    complete_effect = {
		add_manpower = -750
		add_equipment_to_stockpile = {
			type = infantry_equipment
			amount = -750
		}
		#hidden_effect = {
		#	FROM = { 
		#		set_state_flag = Coring_In_Progress
		#	}
		#}
	}
	remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			FROM = {
				add_core_of = ROOT
				#clr_state_flag = Coring_In_Progress
			}
	}
}

BFN_coring_states_targeted_decision_remote_at_war = {
	icon = GFX_decision_SWE_set_navy_budget
    state_target = yes
    target_root_trigger = {
    }
    target_array = controlled_states
    target_trigger = {
        FROM = {
			NOT = { is_core_of = ROOT }
			is_fully_controlled_by = ROOT
			is_owned_by = ROOT
			is_coastal = yes
			all_neighbor_state = {
				NOT = {
					AND = {
						is_core_of = ROOT
						is_owned_by = ROOT
					}
				}
			}
        }
    }
	available = {
		has_war = yes
		num_of_civilian_factories_available_for_projects > 29
		has_manpower > 2999
		has_equipment = {
			infantry_equipment > 2999
		}
		has_equipment = {
			convoy > 149
		}
	}
	visible = {
			has_war = yes
            NOT = { has_country_flag = show_integration_decisions }
			FROM = { 
				NOT = { is_core_of = ROOT } 
				#NOT = { has_state_flag = Coring_In_Progress } 
			}
        }
	ai_will_do = {
			factor = 0
			modifier = {
				factor = 0
				has_political_power < 600
			}
			modifier = {
				factor = 0
				has_equipment = {
					convoy < 1000
				}
			}
		}	
	modifier = { 
		civilian_factory_use = 30
		#stability_weekly = -0.006
	}
	days_remove = 180
    on_map_mode = map_and_decisions_view
    cost = 150
    complete_effect = {
		add_manpower = -3000
		add_equipment_to_stockpile = {
			type = infantry_equipment
			amount = -3000
		}
		add_equipment_to_stockpile = {
			type = convoy
			amount = -150
		}
		#hidden_effect = {
		#	FROM = { 
		#		set_state_flag = Coring_In_Progress
		#	}
		#}
	}
	remove_effect = {
			if = { limit = { is_ai = no } hidden_effect = { sound_effect = complete_focus } }
			FROM = {
				add_core_of = ROOT
				#clr_state_flag = Coring_In_Progress
			}
	}
}

BFN_Claim_CRO = {
		icon = eng_trade_unions_support
		
		allowed = {
			NOT = { tag = CRO }
		}

		available = {
			date > 1936.1.1
			date < 1949.1.1
			has_war_with = CRO
			country_exists = CRO
			surrender_progress < 0.25
			is_subject = no
			GOT_Can_Claim_CRO = yes
		}

		visible = {
			GOT_Can_Claim_CRO = yes
			has_war_with = CRO
			country_exists = CRO
			NOT = { has_country_flag = show_integration_decisions }
		}

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		complete_effect = {
			country_event = { id = GOT_Location_Country.31 }
		}
		
	}
	
	BFN_Claim_STO = {
		icon = eng_trade_unions_support
		
		allowed = {
			NOT = { tag = STO }
		}

		available = {
			date > 1936.1.1
			date < 1949.1.1
			has_war_with = STO
			country_exists = STO
			surrender_progress < 0.25
			is_subject = no
			GOT_Can_Claim_STO = yes
		}

		visible = {
			GOT_Can_Claim_STO = yes
			has_war_with = STO
			country_exists = STO
			NOT = { has_country_flag = show_integration_decisions }
		}

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		complete_effect = {
			country_event = { id = GOT_Location_Country.34 }
		}
		
	}
	
	BFN_Claim_DRA = {
		icon = eng_trade_unions_support
		
		allowed = {
			NOT = { tag = DRA }
		}

		available = {
			date > 1936.1.1
			date < 1949.1.1
			has_war_with = DRA
			country_exists = DRA
			surrender_progress < 0.25
			is_subject = no
			GOT_Can_Claim_DRA = yes
		}

		visible = {
			GOT_Can_Claim_DRA = yes
			has_war_with = DRA
			country_exists = DRA
			NOT = { has_country_flag = show_integration_decisions }
		}

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		complete_effect = {
			country_event = { id = GOT_Location_Country.39 }
		}
		
	}
	
	BFN_Claim_WES = {
		icon = eng_trade_unions_support
		
		allowed = {
			NOT = { tag = WES }
		}

		available = {
			date > 1936.1.1
			date < 1949.1.1
			has_war_with = WES
			country_exists = WES
			surrender_progress < 0.25
			is_subject = no
			GOT_Can_Claim_WES = yes
		}

		visible = {
			GOT_Can_Claim_WES = yes
			has_war_with = WES
			country_exists = WES
			NOT = { has_country_flag = show_integration_decisions }
		}

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		complete_effect = {
			country_event = { id = GOT_Location_Country.32 }
		}
		
	}
	
	BFN_Claim_NOR = {
		icon = eng_trade_unions_support
		
		allowed = {
			NOT = { tag = NOR }
		}

		available = {
			date > 1936.1.1
			date < 1949.1.1
			has_war_with = NOR
			country_exists = NOR
			surrender_progress < 0.25
			is_subject = no
			GOT_Can_Claim_NOR = yes
		}

		visible = {
			GOT_Can_Claim_NOR = yes
			has_war_with = NOR
			country_exists = NOR
			NOT = { has_country_flag = show_integration_decisions }
		}

		cost = 25

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		complete_effect = {
			country_event = { id = GOT_Location_Country.33 }
		}
		
	}

}

#Baratheon Empire
FORM_Baratheon_Empire_Decisions = {

	#Baratheon Empire
	RON_Formable_Decision1 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = CRO
				original_tag = DRA
				original_tag = STO
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Baratheon_Empire2 }
		}
		available = {
			OR = {
				original_tag = CRO
				original_tag = DRA
				original_tag = STO
			}
			custom_trigger_tooltip = {
				tooltip = GOT_Baratheon_Ruler_tt
				GOT_Baratheon_Ruler = yes
			}
			NOT = { has_global_flag = FORM_Baratheon_Empire2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_STO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = STO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_DRA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = DRA
						}
						is_controlled_by = FROM
					}
				}
			}
			
			
			
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.1
			}
			hidden_effect = {
				set_global_flag = FORM_Baratheon_Empire2
			}
		}
	}
	
}	

#Isles and Rivers
FORM_IRO_RIV_Decisions = {

	#Isles and Rivers
	RON_Formable_Decision2 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = IRO
				original_tag = RIV
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_IRO_RIV2 }
		}
		available = {
			OR = {
				original_tag = IRO
				original_tag = RIV
			}
			NOT = { has_global_flag = FORM_IRO_RIV2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_IRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = IRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_RIV_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = RIV
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.2
			}
			hidden_effect = {
				set_global_flag = FORM_IRO_RIV2
			}
		}
	}
	
}

#Lannister Empire
FORM_Lannister_Empire_Decisions = {

	#Lannister Empire
	RON_Formable_Decision4 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = CRO
				original_tag = WES
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Lannister_Empire2 }
		}
		available = {
			OR = {
				original_tag = CRO
				original_tag = WES
			}
			custom_trigger_tooltip = {
				tooltip = GOT_Lannister_Ruler_tt
				GOT_Lannister_Ruler = yes
			}
			NOT = { has_global_flag = FORM_Lannister_Empire2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WES_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WES
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.4
			}
			hidden_effect = {
				set_global_flag = FORM_Lannister_Empire2
			}
		}
	}
	
}


#Lannister-Baratheon Empire
FORM_Lannister_Baratheon_Empire_Decisions = {

	#Lannister-Baratheon Empire
	RON_Formable_Decision3 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = CRO
				original_tag = WES
				original_tag = STO
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Lannister_Baratheon_Empire2 }
		}
		available = {
			OR = {
				original_tag = CRO
				original_tag = WES
				original_tag = STO
			}
			custom_trigger_tooltip = {
				tooltip = GOT_Lannister_Baratheon_Ruler_tt
				GOT_Lannister_Baratheon_Ruler = yes
			}
			NOT = { has_global_flag = FORM_Lannister_Baratheon_Empire2 }
			#is_subject = no
			
			custom_trigger_tooltip = {
				tooltip = RON_Controls_STO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = STO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_DRA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = DRA
						}
						is_controlled_by = FROM
					}
				}
			}
			
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WES_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WES
						}
						is_controlled_by = FROM
					}
				}
			}
			
			
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.3
			}
			hidden_effect = {
				set_global_flag = FORM_Lannister_Baratheon_Empire2
			}
		}
	}
	
}

#North and Trident
FORM_NOR_RIV_Decisions = {

	#North and Trident
	RON_Formable_Decision5 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = NOR
				original_tag = RIV
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_NOR_RIV2 }
		}
		available = {
			OR = {
				original_tag = NOR
				original_tag = RIV
			}
			NOT = { has_global_flag = FORM_NOR_RIV2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_NOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = NOR
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_RIV_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = RIV
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.5
			}
			hidden_effect = {
				set_global_flag = FORM_NOR_RIV2
			}
		}
	}
	
}

FORM_Targaryen_Empire_Decisions = {

	#Targaryen Empire
	RON_Formable_Decision6 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			original_tag = TAR
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Targaryen_Empire2 }
		}
		available = {
			original_tag = TAR
			NOT = { has_global_flag = FORM_Targaryen_Empire2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_SLA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = AST
						}
						is_controlled_by = FROM
					}
				}
				all_state = {
					if = {
						limit = {
							is_core_of = YUN
						}
						is_controlled_by = FROM
					}
				}
				all_state = {
					if = {
						limit = {
							is_core_of = MER
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_DRA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = DRA
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.6
			}
			hidden_effect = {
				set_global_flag = FORM_Targaryen_Empire2
			}
		}
	}
}

#True North
FORM_True_North_Decisions = {

	#True North
	RON_Formable_Decision7 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = NOR
				original_tag = NWA
				original_tag = WIL
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_True_North2 }
		}
		available = {
			OR = {
				original_tag = NOR
				original_tag = NWA
				original_tag = WIL
			}
			NOT = { has_global_flag = FORM_True_North2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_NOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = NOR
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_NWA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = NWA
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WIL_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WIL
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.7
			}
			hidden_effect = {
				set_global_flag = FORM_True_North2
			}
		}
	}
	
}

#Westeros
FORM_Westeros_Decisions = {

	
	#Six Kingdoms
	RON_Formable_Decision8 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = DOR
				original_tag = REA
				original_tag = STO
				original_tag = WES
				original_tag = CRO
				original_tag = RIV
				original_tag = VAL
				original_tag = IRO
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Six_Kingdoms2 }
		}
		available = {
			OR = {
				original_tag = DOR
				original_tag = REA
				original_tag = STO
				original_tag = WES
				original_tag = CRO
				original_tag = RIV
				original_tag = VAL
				original_tag = IRO
			}
			NOT = { has_global_flag = FORM_Six_Kingdoms2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_DOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = DOR
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_REA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = REA
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_STO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = STO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WES_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WES
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_RIV_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = RIV
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_VAL_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = VAL
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_IRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = IRO
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.8
			}
			hidden_effect = {
				set_global_flag = FORM_Six_Kingdoms2
			}
		}
	}


	#Seven Kingdoms
	RON_Formable_Decision9 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = DOR
				original_tag = REA
				original_tag = STO
				original_tag = WES
				original_tag = CRO
				original_tag = RIV
				original_tag = VAL
				original_tag = IRO
				original_tag = NOR
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Seven_Kingdoms2 }
		}
		available = {
			OR = {
				original_tag = DOR
				original_tag = REA
				original_tag = STO
				original_tag = WES
				original_tag = CRO
				original_tag = RIV
				original_tag = VAL
				original_tag = IRO
				original_tag = NOR
			}
			NOT = { has_global_flag = FORM_Seven_Kingdoms2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_DOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = DOR
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_REA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = REA
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_STO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = STO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WES_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WES
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_RIV_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = RIV
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_VAL_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = VAL
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_IRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = IRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_NOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = NOR
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.9
			}
			hidden_effect = {
				set_global_flag = FORM_Seven_Kingdoms2
			}
		}
	}


	#Westeros
	RON_Formable_Decision10 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = DOR
				original_tag = REA
				original_tag = STO
				original_tag = WES
				original_tag = CRO
				original_tag = RIV
				original_tag = VAL
				original_tag = IRO
				original_tag = NOR
				original_tag = NWA
				original_tag = WIL
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Westeros2 }
		}
		available = {
			OR = {
				original_tag = DOR
				original_tag = REA
				original_tag = STO
				original_tag = WES
				original_tag = CRO
				original_tag = RIV
				original_tag = VAL
				original_tag = IRO
				original_tag = NOR
				original_tag = NWA
				original_tag = WIL
			}
			NOT = { has_global_flag = FORM_Westeros2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_DOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = DOR
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_REA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = REA
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_STO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = STO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WES_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WES
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_CRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = CRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_RIV_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = RIV
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_VAL_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = VAL
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_IRO_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = IRO
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_NOR_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = NOR
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_NWA_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = NWA
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_WIL_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = WIL
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.10
			}
			hidden_effect = {
				set_global_flag = FORM_Westeros2
			}
		}
	}
	
}

#Valyrian Freehold
FORM_Valyrian_Freehold_Decisions = {

	#Valyrian Freehold
	RON_Formable_Decision11 = {
		ai_will_do = {
			factor = 1000
		}
		cost = 10
		fire_only_once = yes
		visible = {
			OR = {
				original_tag = TAR
				original_tag = VLY
				original_tag = MAN
			}
			NOT = { has_country_flag = show_formables_decisions }
			NOT = { has_global_flag = FORM_Valyrian_Freehold2 }
		}
		available = {
			OR = {
				original_tag = TAR
				original_tag = VLY
				original_tag = MAN
			}
			NOT = { has_global_flag = FORM_Valyrian_Freehold2 }
			#is_subject = no
			custom_trigger_tooltip = {
				tooltip = RON_Controls_MAN_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = MAN
						}
						is_controlled_by = FROM
					}
				}
			}
			custom_trigger_tooltip = {
				tooltip = RON_Controls_VLY_States_tt
				all_state = {
					if = {
						limit = {
							is_core_of = VLY
						}
						is_controlled_by = FROM
					}
				}
			}
			

		}
		icon = generic_form_nation
		complete_effect = {
			country_event = {
				id = RON_Formable_Country.11
			}
			hidden_effect = {
				set_global_flag = FORM_Valyrian_Freehold2
			}
		}
	}
	
}