
GOT_Game_Decisions = {

	TAR_conquer_Essos_countries = {
	
		icon = generic_army_support
		
		allowed = {
			original_tag = TAR
		}
	
        visible = {
			original_tag = TAR
			NOT = { has_country_flag = EW_Operation }
			FROM = { 
				is_neighbor_of = TAR
			}
			has_global_flag = GOT_Targaryen_Stay_In_Pentos_Flag
		}
        target_trigger = {
            FROM = {
                NOT = { has_war_with = ROOT }
				NOT = { is_in_faction_with = ROOT }
				GOT_Essos_Countries = yes
            }
        }
        cost = 0
		modifier = { }
		
		ai_will_do = {
			factor = 1000
		}
		
		days_remove = 7
		war_with_on_remove = FROM

        available = {
		
			surrender_progress < 0.1
		
			has_global_flag = GOT_Targaryen_Stay_In_Pentos_Flag
			
			FROM = { 
				is_neighbor_of = TAR
			}

        }
		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = FROM
			}
		}
    }
	
	TAR_conquer_Westeros_countries = {
	
		icon = generic_army_support
		
		allowed = {
			original_tag = TAR
		}
	
        visible = {
			original_tag = TAR
			NOT = { has_country_flag = EW_Operation }
			OR = {
				FROM = { 
					is_neighbor_of = TAR
				}
				any_other_country = {
					is_in_faction_with = TAR
					is_neighbor_of = FROM
				}
			}
			owns_state = 142
		}
        target_trigger = {
            FROM = {
                NOT = { has_war_with = ROOT }
				NOT = { is_in_faction_with = ROOT }
				GOT_Westeros_Countries = yes
            }
        }
        cost = 0
		modifier = { }
		
		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1944.1.1
			}
			modifier = {
				factor = 0
				has_country_leader = {
					name = "Jon Snow"
					ruling_only = yes
				}
			}
		}
		
		days_remove = 7
		war_with_on_remove = FROM

        available = {
		
			surrender_progress < 0.1
			
			OR = {
				FROM = { 
					is_neighbor_of = TAR
				}
				any_other_country = {
					is_in_faction_with = TAR
					is_neighbor_of = FROM
				}
			}
			
			owns_state = 142

        }
		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = FROM
			}
		}
    }

	TAR_Legitimize_Gendry_Baratheon = {

		icon = generic_army_support

		visible = {
			OR = {
				STO = {
					exists = no
				}
				STO = {
					has_war_with = STO
				}
			}
		}

		fire_only_once = yes

		available = {
			is_subject = no
			
			OR = {
				STO = {
					exists = no
				}
				STO = {
					has_capitulated = yes
					has_war_with = STO
				}
			}
			
			custom_trigger_tooltip = {
				tooltip = TAR_Stormlands_Controlled_By_Us_tt
				32 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				133 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				150 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				130 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				116 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				118 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				128 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				132 = {
					is_controlled_by_ROOT_or_subject = yes
				}
			}
		}

		highlight_states = {
			highlight_state_targets = {
				state = 32
				state = 133
				state = 150
				state = 130
				state = 116
				state = 118
				state = 128
				state = 132
			}
		}

		cost = 25

		ai_will_do = {
			base = 1000
		}

		complete_effect = {

			country_event = {
				id = GOT_Story_Country.726
			}	

		}
	}
	
	TAR_Legitimize_Tyrion_Lannister = {

		icon = generic_army_support

		visible = {
			OR = {
				WES = {
					exists = no
				}
				WES = {
					has_war_with = WES
				}
			}
		}

		fire_only_once = yes

		available = {
			is_subject = no
			
			OR = {
				WES = {
					exists = no
				}
				WES = {
					has_capitulated = yes
					has_war_with = WES
				}
			}
			
			custom_trigger_tooltip = {
				tooltip = TAR_Westerlands_Controlled_By_Us_tt
				105 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				98 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				85 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				197 = {
					is_controlled_by_ROOT_or_subject = yes
				}
				31 = {
					is_controlled_by_ROOT_or_subject = yes
				}
			}
		}

		highlight_states = {
			highlight_state_targets = {
				state = 105
				state = 98
				state = 85
				state = 197
				state = 31
			}
		}

		cost = 25

		ai_will_do = {
			base = 1000
		}

		complete_effect = {

			country_event = {
				id = GOT_Story_Country.748
			}	

		}
	}

	TAR_Mine_For_Dragonglass = {
		icon = generic_army_support
		
		allowed = {
			# Scope to relevant tags
		}

		available = {
			has_global_flag = GOT_Dragonglass_Found_Flag
			owns_state = 108
			controls_state = 108
			country_exists = ICE
			has_war_with = ICE
		}

		visible = {
			owns_state = 108
			country_exists = ICE
		}

		cost = 25
		
		# Active ONLY during the 90 days while mining and distribution are active
		modifier = {
			civilian_factory_use = 5
		}

		targeted_modifier = {
			tag = ICE
			attack_bonus_against = 0.25
		}

		# Allows the decision to be taken again immediately after the 90 days expire
		days_re_enable = 0

		ai_will_do = {
			factor = 1000
		}

		days_remove = 90

		complete_effect = {
			# Runs every time the 90-day cycle finishes
		}
	}

	ICE_conquer_countries = {
	
		icon = generic_army_support
		
		allowed = {
			original_tag = ICE
		}
	
        visible = {
			original_tag = ICE
			NOT = { has_country_flag = EW_Operation }
			FROM = { 
				is_neighbor_of = ICE
			}
		}
        target_trigger = {
            FROM = {
                NOT = { has_war_with = ROOT }
				NOT = { is_in_faction_with = ROOT }
            }
        }
        cost = 0
		modifier = { }
		
		ai_will_do = {
			factor = 1000
		}
		
		days_remove = 7
		war_with_on_remove = FROM

        available = {
		
			surrender_progress < 0.1
		
			OR = {
				has_global_flag = GOT_Wall_Shattered_Flag
				controls_state = 25
				controls_state = 18
				controls_state = 24
			}
			
			FROM = { 
				is_neighbor_of = ICE
			}

        }
		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = FROM
			}
		}
    }

	LFC_Create_League = {
		icon = generic_army_support

		available = { 
			NOT = { has_global_flag = GOT_League_of_Free_Cities_Flag }
			OR = {
				tag = BRA
				tag = PEN
				tag = VOL
			}
			date < 1950.1.1
			is_in_faction = no
			has_offensive_war = no
			threat > 0.30
			
			# Active Justification / Wargoal Check
			# Checks if ANY country is justifying or holds a wargoal against any Free City
			any_other_country = {
				OR = {
					is_justifying_wargoal_against = BRA
					has_wargoal_against = BRA
					
					is_justifying_wargoal_against = LOR
					has_wargoal_against = LOR
					
					is_justifying_wargoal_against = NRV
					has_wargoal_against = NRV
					
					is_justifying_wargoal_against = QOH
					has_wargoal_against = QOH
					
					is_justifying_wargoal_against = PEN
					has_wargoal_against = PEN
					
					is_justifying_wargoal_against = MYR
					has_wargoal_against = MYR
					
					is_justifying_wargoal_against = TYR
					has_wargoal_against = TYR
					
					is_justifying_wargoal_against = LYS
					has_wargoal_against = LYS
					
					is_justifying_wargoal_against = VOL
					has_wargoal_against = VOL
				}
			}
		}
		
		allowed = {
			OR = {
				tag = BRA
				tag = PEN
				tag = VOL
			}
		}

		visible = {
			NOT = { has_global_flag = GOT_League_of_Free_Cities_Flag }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}
		
		days_remove = 7

		complete_effect = {
			set_global_flag = GOT_League_of_Free_Cities_Flag
		}
		
		remove_effect = {
			country_event = { id = GOT_Faction_Country.1 hours = 6 }
		}
	}

	CRO_Crownlands_Invasion_of_RIV = {
		icon = generic_army_support
		
		allowed = {
			tag = CRO
		}

		available = {
			tag = CRO
			has_war_with = NOR
			NOT = { has_war_with = RIV }
			WES = { has_war_with = RIV }
			NOT = { is_in_faction_with = RIV }
			is_in_faction_with = WES
			country_exists = RIV
			surrender_progress < 0.1
			date < 1938.1.1
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 7
		war_with_on_remove = RIV

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = RIV
				expire = 365
			}
			if = {
				limit = {
					is_ai = yes 
					NOT = { has_war_with = RIV }
					NOT = { is_in_faction_with = RIV }
				}
				declare_war_on = {
					target = RIV
					type = annex_everything
				}
			}
		}
	}

	RIV_join_northern_faction = {
		icon = generic_army_support

		available = { 
			tag = RIV
			date < 1950.1.1
			NOT = { has_war_with = NOR }
			NOR = { is_faction_leader = yes }
			is_in_faction = no
			has_war_with = WES
			OR = { 
				NOR = { has_war_with = WES } 
				NOR = { has_wargoal_against = WES }
			}
		}
		
		allowed = {
			tag = RIV
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { has_country_flag = EW_Ironborn_Invasion }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1936.6.25
			}
		}

		days_remove = 7

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			NOR = {
				country_event = generic.2
			}
		}
	}

	DRA_battle_of_blackwater_bay = {
		icon = generic_army_support

		available = { 
			tag = DRA
			date > 1937.1.1
			date < 1945.1.1
			surrender_progress < 0.1
			has_war_with = CRO
			142 = { CONTROLLER = { has_war_with = ROOT } }
			NOT = { has_country_flag = EW_Battle_of_Blackwater_Bay }
			has_country_leader = {
				name = "Stannis Baratheon"
				ruling_only = yes
			}
		}
		
		allowed = {
			tag = DRA
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { has_country_flag = EW_Battle_of_Blackwater_Bay }
		}

		cost = 0

		fire_only_once = yes
		
		targeted_modifier = {
			tag = CRO
			attack_bonus_against = 0.25
		}
		
		targeted_modifier = {
			tag = WES
			attack_bonus_against = 0.25
		}
		
		modifier = { 
			army_org_factor = 0.25
			breakthrough_factor = 0.25
			naval_invasion_planning_bonus_speed = 0.25
			navy_org_factor = 0.25
			naval_morale_factor = 0.25
			naval_strike_targetting_factor = 0.25
		}

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1937.10.3
			}
			modifier = {
				factor = 0
				NOT = { is_in_faction_with = STO }
				NOT = { controls_state = 116 }
			}
		}

		days_remove = 14

		complete_effect = {
			set_country_flag = EW_Operation
			set_country_flag = EW_Battle_of_Blackwater_Bay
			DRA = {
				country_event = { id = GOT_Wars_Country.1 days = 1 random_days = 7 }	
			}
			every_country = {
				limit = { 
					OR = { 
						tag = CRO
						is_in_faction_with = CRO
					} 
				}
				country_event = { id = GOT_Wars_Country.2 hours = 6 }
			}
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
		}
	}
	
	DRA_recruit_jon_snow = {
		icon = generic_army_support

		available = { 
			tag = DRA
			date < 1950.1.1
			surrender_progress < 0.1
			has_war_with = BOL
			OR = {
				NOT = { has_war_with = WIL }
				WIL = { has_capitulated = yes }
			}
			NOT = { has_war_with = NWA }
			NOT = { has_global_flag = GOT_Second_Northern_Civil_War_Flag }
			has_country_leader = {
				name = "Stannis Baratheon"
				ruling_only = yes
			}
			NWA = { 
				has_country_leader = {
					name = "Jon Snow"
					ruling_only = yes
				}
			}
			OR = {
				BOL = { owns_state = 123 }
				BOL = { 123 = { is_controlled_by_ROOT_or_ally = yes } }
			}
			OR = {
				NOR = { exists = no }
				NOR = { is_subject = yes }
			}
			is_in_faction = yes
			is_faction_leader = yes
		}
		
		allowed = {
			tag = DRA
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 7

		complete_effect = {
			set_country_flag = EW_Operation
			NWA = {
				country_event = { id = GOT_Diplomatic_Country.81 days = 1 random_days = 7 }	
			}
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
		}
	}
	
	IRO_invasion_of_the_north = {
		icon = generic_army_support

		available = { 
			tag = IRO
			date > 1937.1.1
			date < 1945.1.1
			surrender_progress < 0.1
			has_war_with = NOR
			NOT = { has_country_flag = EW_Ironborn_Invasion }
		}
		
		allowed = {
			tag = IRO
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { has_country_flag = EW_Ironborn_Invasion }
		}

		cost = 0

		fire_only_once = yes
		
		targeted_modifier = {
			tag = NOR
			attack_bonus_against = 0.25
		}
		
		targeted_modifier = {
			tag = RIV
			attack_bonus_against = 0.25
		}
		
		modifier = { 
			army_org_factor = 0.25
			breakthrough_factor = 0.25
			naval_invasion_planning_bonus_speed = 0.25
			navy_org_factor = 0.25
			naval_morale_factor = 0.25
			naval_strike_targetting_factor = 0.25
		}

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1937.4.8
			}
		}

		days_remove = 14

		complete_effect = {
			set_country_flag = EW_Operation
			set_country_flag = EW_Ironborn_Invasion
			IRO = {
				country_event = { id = GOT_Wars_Country.3 days = 1 random_days = 7 }	
			}
			every_country = {
				limit = { 
					OR = { 
						tag = NOR
						is_in_faction_with = NOR
					} 
				}
				country_event = { id = GOT_Wars_Country.4 hours = 6 }
			}
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
		}
	}
	
	IRO_invasion_of_winterfell = {
		icon = generic_army_support

		available = { 
			tag = IRO
			date > 1937.1.1
			date < 1945.1.1
			surrender_progress < 0.1
			has_war_with = NOR
			has_country_flag = GOT_Theon_Returns_Flag
			NOT = { has_country_flag = EW_Ironborn_Invasion_Winterfell }
			
			any_state = {
				is_controlled_by = IRO
				is_core_of = NOR
			}
			
		}
		
		allowed = {
			tag = IRO
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { has_country_flag = EW_Ironborn_Invasion_Winterfell }
		}

		cost = 0

		fire_only_once = yes
		
		targeted_modifier = {
			tag = NOR
			attack_bonus_against = 0.25
		}
		
		targeted_modifier = {
			tag = RIV
			attack_bonus_against = 0.25
		}
		
		modifier = { 
			army_org_factor = 0.25
			breakthrough_factor = 0.25
		}

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1937.6.25
			}
		}

		days_remove = 14

		complete_effect = {
			set_country_flag = EW_Operation
			set_country_flag = EW_Ironborn_Invasion_Winterfell
			IRO = {
				country_event = { id = GOT_Story_Country.125 hours = 6 }	
			}
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
		}
	}
	
	STO_Stormlands_Invasion_of_CRO = {
		icon = generic_army_support
		
		allowed = {
			tag = STO
		}

		available = {
			tag = STO
			date > 1937.1.1
			date < 1938.1.5
			NOT = { has_war_with = CRO }
			NOT = { is_in_faction_with = CRO }
			country_exists = CRO
			surrender_progress < 0.1
			has_country_flag = GOT_Hosted_Tournament_Flag
			has_country_flag = GOT_Claimed_Iron_Throne_Flag
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1937.6.15
			}
		}

		days_remove = 7
		war_with_on_remove = CRO

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = CRO
				expire = 365
			}
			if = {
				limit = {
					is_ai = yes 
					NOT = { has_war_with = CRO }
					NOT = { is_in_faction_with = CRO }
				}
				declare_war_on = {
					target = CRO
					type = annex_everything
				}
			}
		}
	}
	
	TAR_Targaryen_Invasion_of_DRA = {
		icon = generic_army_support
		
		allowed = {
			tag = TAR
		}

		available = {
			tag = TAR
			date > 1941.1.1
			108 = {
				OWNER = {
					NOT = { is_in_faction_with = TAR }
				}
			}
			surrender_progress < 0.1
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.11.15
			}
		}

		days_remove = 7
		#war_with_on_remove = DRA

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			TAR = {
				country_event = { id = GOT_Story_Country.550 hours = 6 }	
			}
		}
	}
	
	TAR_land_at_dragonstone = {
		icon = generic_army_support

		available = { 
			tag = TAR
			date > 1942.1.1
			surrender_progress < 0.1
			#has_war_with = DRA
			has_global_flag = GOT_Dany_Sails_West_Flag
			108 = { OWNER = { has_war_with = ROOT } }
			NOT = { has_country_flag = EW_Land_at_Dragonstone }
		}
		
		allowed = {
			tag = TAR
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { has_country_flag = EW_Land_at_Dragonstone }
		}

		cost = 0

		fire_only_once = yes
		
		targeted_modifier = {
			tag = DRA
			attack_bonus_against = 0.25
		}
		
		modifier = { 
			army_org_factor = 0.25
			breakthrough_factor = 0.25
			naval_invasion_planning_bonus_speed = 0.25
			navy_org_factor = 0.25
			naval_morale_factor = 0.25
			naval_strike_targetting_factor = 0.25
		}

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.1.1
			}
		}

		days_remove = 14

		complete_effect = {
			set_country_flag = EW_Operation
			set_country_flag = EW_Land_at_Dragonstone
			TAR = {
				country_event = { id = GOT_Wars_Country.5 days = 1 random_days = 7 }	
			}
			108 = { 
				OWNER = { 
					country_event = { id = GOT_Wars_Country.6 hours = 6 }
				}
			}
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
		}
	}
	
	PEN_End_Targaryen_Asylum = {
		icon = generic_army_support
		
		allowed = {
			tag = PEN
		}

		available = {
			tag = PEN
			date > 1936.1.1
			date < 1950.1.1
			NOT = { has_war_with = TAR }
			country_exists = TAR
			surrender_progress < 0.1
			TAR = { owns_state = 167 }
			TAR = { 
				any_owned_state = {
					NOT = { state = 167 }
				}
			}
			is_subject = no
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 7
		war_with_on_remove = TAR

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			diplomatic_relation = {
				country = TAR
				relation = guarantee
				active = no
			}
			TAR = {
				country_event = { id = GOT_Diplomatic_Country.43 }
			}
		}
	}
	
	TAR_Burn_The_Khals = {
		icon = generic_army_support
		
		allowed = {
			tag = TAR
		}

		available = {
			tag = TAR
			date > 1941.1.1
			#NOT = { has_war_with = DOT }
			country_exists = DOT
			surrender_progress < 0.1
			is_subject = no
			has_country_leader = {
				name = "Daenerys Targaryen"
				ruling_only = yes
			}
			controls_state = 89
			has_global_flag = GOT_TAR_DOT_Wedding_Flag
			DOT = {
				NOT = { 
					has_country_leader = {
						name = "Daenerys Targaryen"
						ruling_only = yes
					}
				}
			}
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.4.15
			}
		}

		days_remove = 7
		war_with_on_remove = DOT

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			country_event = { id = GOT_Story_Country.517 hours = 1 }
		}
	}
	
	TAR_Invasion_of_CRO = {
		icon = generic_army_support
		
		allowed = {
			tag = TAR
		}

		available = {
			tag = TAR
			date > 1942.1.1
			NOT = { has_war_with = CRO }
			NOT = { is_in_faction_with = CRO }
			country_exists = CRO
			surrender_progress < 0.1
			108 = { is_controlled_by_ROOT_or_ally = yes }
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.1.1
			}
		}

		days_remove = 7
		war_with_on_remove = CRO

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = CRO
				expire = 365
			}
			if = {
				limit = {
					is_ai = yes 
					NOT = { has_war_with = CRO }
					NOT = { is_in_faction_with = CRO }
				}
				declare_war_on = {
					target = CRO
					type = annex_everything
				}
			}
		}
	}
	
	TAR_Invasion_of_CRO2 = {
		icon = generic_army_support
		
		allowed = {
			tag = TAR
		}

		available = {
			tag = TAR
			date > 1943.1.1
			NOT = { has_war_with = CRO }
			NOT = { is_in_faction_with = CRO }
			country_exists = CRO
			surrender_progress < 0.1
			108 = { is_controlled_by_ROOT_or_ally = yes }
			NOT = { country_exists = ICE }
			NOT = { has_truce_with = CRO }
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { country_exists = ICE }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1943.1.1
			}
		}

		days_remove = 7
		war_with_on_remove = CRO

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = CRO
				expire = 365
			}
			if = {
				limit = {
					is_ai = yes 
					NOT = { has_war_with = CRO }
					NOT = { is_in_faction_with = CRO }
				}
				declare_war_on = {
					target = CRO
					type = annex_everything
				}
			}
		}
	}
	
	TAR_purge_targaryens = {
		icon = generic_army_support
		
		allowed = {
			tag = CRO
		}

		available = {
			tag = CRO
			date > 1943.1.1
			NOT = { has_war_with = TAR }
			NOT = { is_in_faction_with = TAR }
			country_exists = TAR
			surrender_progress < 0.1
			TAR = { 108 = { is_controlled_by_ROOT_or_ally = yes } }
			NOT = { country_exists = ICE }
			NOT = { has_truce_with = TAR }
		}

		visible = {
			NOT = { has_country_flag = EW_Operation }
			NOT = { country_exists = ICE }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 0
		}

		days_remove = 7
		war_with_on_remove = TAR

		complete_effect = {
			set_country_flag = EW_Operation
		}
		
		remove_effect = {
			clr_country_flag = EW_Operation
			create_wargoal = {
				type = annex_everything
				target = TAR
				expire = 365
			}
			if = {
				limit = {
					is_ai = yes 
					NOT = { has_war_with = TAR }
					NOT = { is_in_faction_with = TAR }
				}
				declare_war_on = {
					target = TAR
					type = annex_everything
				}
			}
		}
	}
	
	GOT_Give_GOT_Mode_To_New_Countries = {
		
		allowed = {
		}

		available = {
			has_global_flag = GOT_Mode_GOT
			any_country = {
				exists = yes
				NOT = { 
					has_idea = GOT_Mode_GOT 
				}
				NOT = { 
					has_idea = GOT_Mode_GOT_Non_BBA
				}
			}
		}
		
		visible = {
			has_global_flag = GOT_Mode_GOT
			is_ai = yes
		}

		cost = 0
		
		days_re_enable = 30

		ai_will_do = {
			factor = 1000
		}

		complete_effect = {
			every_country = {
				limit = {
					exists = yes
					NOT = { 
						has_idea = GOT_Mode_GOT 
					}
					NOT = { 
						has_idea = GOT_Mode_GOT_Non_BBA 
					}
				}
				if = {
					limit = {
						has_dlc = "By Blood Alone"
					}
					add_ideas = GOT_Mode_GOT
				}
				else = {
					add_ideas = GOT_Mode_GOT_Non_BBA
				}
				country_event = { id = Game_Event.4 }
			}
		}
	}
}
